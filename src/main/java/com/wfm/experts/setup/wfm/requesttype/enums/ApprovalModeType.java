package com.wfm.experts.setup.wfm.requesttype.enums;

/**
 * Defines the automatic action for unapproved requests.
 * The values MUST BE UPPERCASE to match the frontend model.
 */
public enum ApprovalModeType {
    AUTO_APPROVE,
    REQUIRES_APPROVAL
}